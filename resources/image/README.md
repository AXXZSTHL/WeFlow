> Windows: 使用 img_helper.dll (koffi FFI 调用) 注入微信进程
> macOS:   使用 img_helper.dylib (koffi FFI 调用) 注入微信进程
>   编译: cd resources/image/macos && chmod +x build.sh && ./build.sh
>   前提: 需要在「系统设置 → 隐私与安全性 → 辅助功能」中授权终端
> Linux:   暂未适配